# Prepare — martech-job-radar 決策記錄

> **版本**：JR-010 · 2026-08-31（待操作事項 14 筆，見文末）
> **編號前綴**：`JR-`，全域遞增、永不重用
> **記錄規則繼承根 `prepare.md`**，本檔只記本專案內部決策；
> 主線決策不複製理由，只記落地細節 + `← D-00X` 指標。

---

### JR-010 · 2026-08-31 · data
- **決策**：**先立不改**。已定位「同一家公司同時刊在兩個管道時，去重鍵可能完全失效」，
  但本期**不動 `mergeChannels.py`**，只把根因寫下來、在報告 §01／§03／§07 三處標示影響範圍
- **實例**：awoo 本期 14 列，最多只對應 **7 個實際職缺**（274 筆裡約 7 筆重複，2.6%）
- **三層根因，每一層單獨看都是對的決策，湊在一起才漏**：
  1. **跨管道對不起來**：去重鍵優先用平台職缺編號（← JR-007）。SHOPLINE 的職稱自帶
     `【TPD0603】` 這類與平台無關的內部代碼，所以 Cake 23＋Yourator 9 能正確併成 32
     （其中 20 筆兩邊都刊）。**awoo 沒有這種代碼**，退回用網址，而兩個平台的網址天生不同
  2. **公司名對不起來**：上游 `martech-trend-agent` 給的是 `awoo`，本專案 Cake 抓到的是
     `awoo 阿物科技`。`normCompany()` 只去法人後綴，這兩個字串永遠不相等
  3. **城市後綴**：`normTitle()` **刻意不動圓括號**（← JR-002），因為 Appier 的
     `(Ad Cloud Platform)` 是不同團隊。但 awoo 的 `（台北辦公室）` 是同一個缺的不同地點
- **為何先立不改**：JR-002 已因舉錯例子被更正過一次（← JR-007）。
  這次三層根因剛查到、只有一家公司觸發、影響 2.6%，**先讓它跑一期看會不會有第二個實例**，
  再決定要改哪一層。急著改任何一層都可能重演「多吃掉 14 筆真實職缺」那種代價
- **候選改法（下期評估用，未定案）**：(a) 公司別名表（把 `awoo` 與 `awoo 阿物科技` 對起來）；
  (b) `normTitle()` 只剝除「純地點／辦公室」形態的括號後綴，其餘括號照舊保留；
  (c) 兩者都做。**(b) 風險最高**——要能分辨「地點括號」與「團隊括號」

### JR-009 · 2026-08-19 · analysis
- **決策**：報告每期手算的**跨期差集（新增／消失）一律以網址為鍵**，職稱只用來顯示。
  標題有變動的另外列成「改標題」一類，**不計入新增或消失**
- **理由**：整併（去重）沒有問題——JR-007 已經是用網址比對。壞的是差集：
  前三期都用「公司＋職稱」當鍵，本期實測同一份資料兩種鍵差 5 筆
  （職稱鍵 +19／−21，網址鍵 **+14／−16**），差的 5 筆全是**同一個網址只改標題**：
  Appier `Senior Digital Advertising Data Analyst` → `Digital Advertising Data Analyst`、
  WebComm `(資深)數據工程師` → `數據工程師`、awoo `SEO 內容文案（嘉義）` → `（嘉義/高雄）`、
  樂播科技 2 筆。**照舊算法本期會寫成「Appier 收掉一個資深分析職、同時新開一個分析職」，
  而那是同一個缺**——正是上游 `/martech-report` 列為必查陷阱的「改標題重貼」
- **棄選**：把差集也寫進程式（`mergeChannels.py` 輸出 diff）。
  現階段差集是分析層的判讀動作、每期要配合刊登日與公司名單一起讀，
  寫死成報表反而會讓人略過那兩層過濾（同 JR-006 的取捨）。**先把鍵定對，程式化之後再說**
- **邊界**：本則只管**跨期差集**。整併鍵不動，仍依 JR-007
- **關聯**：上游同日以 `jobId` 為鍵，天生沒有這個問題——
  這也是為何同一個改標題事件，上游看到 0 筆異動、下游看到 2 筆

### JR-008 · 2026-08-13 · scope
- **決策**：名單分層的**視覺**反轉——`core` 從「沒有徽章」改為**實心 accent 徽章＋左側色條**，
  `service`／`adjacent` 各給可辨識但低調的樣式；§02 表格改為**依 `purity` 分三組**顯示，
  排序按鈕只在組內作用。同時把「六大類歸屬測試」明文寫成收錄準則
- **理由**：用戶問「一直加數據類公司會不會漸漸脫離 MarTech，要不要分類並把 MarTech 高亮」。
  查證後發現分層軸（JR-004 的 `purity`）早就存在，壞的是視覺——
  舊版**只有 `service`／`adjacent` 掛徽章，`core` 什麼都不掛**，
  等於用「沒有標記」代表 MarTech 核心，高亮了錯的東西。
  而且 `.p-service`／`.p-adjacent` 兩個 class **從來沒有對應的 CSS 規則**
  （`template.html` 只定義了 `.ptag`），兩層外觀完全相同、根本分不出來
- **棄選**：把 `adjacent` 拆成兩層（分層更精確，但要改值域守門、重新分類現有 4 家，
  且未來每收一家多一個判斷點）；加一條與 `purity` 正交的 `dataFirst` 軸
  （多一個要維護、要判斷的欄位——加軸之前先把既有的軸用對）
- **落地**：`buildArtifact.py` 的 `PURITY_LABEL` 補 `core`、徽章一律輸出；
  `template.html` 補 `.p-core`／`.p-service`／`.p-adjacent` 三組樣式與 `.gsplit` 組標題；
  排序 JS 改為分組後組內排序，`renumber()` 跳過組標題列
- **邊界**：收錄準則寫進 §02 與 `docs/candidates.md`——
  **能歸進六大類就收，不能就不收**。本期實例：QSearch 過（社群關係）；
  開必拓數據（AI 製造檢測）、緯雲（體育遊戲系統）不過，雖然它們的職缺一直出現在資料裡

### JR-007 · 2026-08-13 · data
- **決策**：整併鍵從「單一鍵比對」改為「**任一識別碼相同即同一個缺**」的分群。
  識別碼＝標題的【CODE】**與**網址內嵌編號（兩個都收）；沒有編號時用平台識別碼，
  **不退回職稱**；完全沒有網址時才用職稱
- **理由**：舊鍵同時犯了兩個方向相反的錯，而且**三期都在錯**——
  ①**漏併**：SHOPLINE `Data Analyst` 在 Cake 標題寫【SGMO0801】、Yourator 寫【TPD0803】，
  兩筆 JD 內文一字不差，是同一個缺卻各算一筆。舊鍵只讀標題編號，
  而 Cake 那筆的網址就寫著 `/jobs/tpd0803data-analyst`——證據一直在網址裡。
  ②**誤併**：Appier `Staff/Senior ML Scientist (Ad Cloud)` 台北（`gh-6897205`）與
  東京（`gh-7092458`）標題相同，沒有編號時舊鍵退回比職稱，兩個真實職缺被併成一筆，
  且勝出者取決於迭代順序，於是跨期比較看到它在台北／東京間跳動
- **棄選**：只信網址編號（Cake 的 slug 也會過期——`Product Manager` 的 slug 停在 `tpd0303`
  而標題已是【TPD1401】，實測會反過來打壞 4 組）；只信標題編號（就是舊行為）；
  用 JD 內文指紋（Appier 的 JD 開頭都是同一段公司介紹，實測 22 組全是誤判）
- **落地**：`mergeChannels.py` 的 `jobCodes()`／`platformId()`／`dedupeTokens()` 與分群迴圈；
  三期資料以新鍵重算：196／266／266（原 196／265／265）
- **邊界**：這也**推翻了 JR-002 舉的例子**——當時說 SGMO0801 與 TPD0803「分屬不同團隊、
  是兩個真實職缺」，實際是同一個缺。JR-002 的決策方向（編號優先於職稱）仍然成立，
  但它引用的證據是錯的。**上一版報告據此寫的「同一個缺換城市會被算成一增一減」也一併收回**，
  那是整併假影不是真實現象

### JR-006 · 2026-08-13 · report
- **決策**：報告新增 §00 推薦職缺，三門檻（口碑 `repScore ≥ 3` 且心得 ≥ 50、`growth ≥ 4`、
  職缺屬數據／AI 類且在台）**同時成立**才進榜；排序用**刊登新鮮度**，
  刊登 > 365 天標「常駐缺」排到最後，**不用四維總分排序**
- **理由**：四維總分是**公司**的分數，職缺不是。拿公司分數排職缺會讓 Appier 那批
  2019-01-09 掛到現在的常駐缺（公司四維 18 分，全榜最高）排在 8/10 才新開的缺前面——
  而報告本身已經判定那批常駐缺不是真的在收人。排序鍵選錯會讓推薦與報告自己的結論打架。
  出處：martech-job-radar/report/template.html:416「掛七年的那兩個是長期收履歷，本期新開的那個才是真的在收人」
- **棄選**：四維總分排序（與 JR-001 結論打架，見上）；
  加權混合分（權重無依據，等於把主觀藏進小數點）；
  只推「近 30 天新開」（本期只有 4 筆，樣本太少到沒有推薦價值）
- **落地**：`buildArtifact.py` 的 `recommendRows()` 與頂部五個門檻常數
  （`REC_*`／`STALE_DAYS`／`FRESH_DAYS`）；`template.html` 的 `#pick` 區塊。
  門檻與排序邏輯全在常數與單一函式裡，改門檻不必動版面
- **邊界**：門檻卡最緊的是「口碑實測」——35 家中只有 9 家有面試趣資料，
  其餘 26 家連參選資格都沒有。所以 A 層只有 2 家，**主因是判斷依據不存在，不是市場只有兩家**。
  這句必須寫在頁面上，否則讀者會把「沒進榜」讀成「不值得投」（同 JR-005 的「—」問題）
- **2026-08-13 修訂（同日，用戶回饋）**：單層門檻讓整張表只剩 2 家，把「查不到」與「不夠好」
  混成同一件事。改為**三層**：A 主推薦（三項齊備）／B 次選（缺一項並標明缺哪一項）／
  觀察名單（公司夠格但本期無範圍內職缺，附原因）。職缺範圍同時放寬到
  TSE 技術顧問與帶資料的產品職——那兩條是報告 §06 自己列的入行路徑，
  排除它們等於推薦與建議自相矛盾。加**管道欄**：同一個缺跨管道整併後只留一列，
  讀者要知道去哪裡投、以及這筆是不是兩個來源交叉確認過的

### JR-005 · 2026-08-06 · report
- **決策**：`hot`／`growth` 查不到證據就**留空**，且未評分的公司**不進四維排名**——
  與 `purity != core` 同一個處理：照列在榜後、`#` 欄顯示「—」，不隱藏
- **理由**：JR-004 補收 15 家時發現 `buildArtifact.py` 寫的是 `c.get("hot", 2)`——
  沒研究過的公司會帶著憑空的 2 分進榜。這正是 JR-001 推翻過三家的那種「有分但無依據」，
  而且比主觀給分更難察覺（沒有人寫過那個 2）
- **棄選**：沿用預設值 2（榜單看起來完整，但前段班會被灌水的新公司擠掉）；
  比照 rep 用中位數代入（rep 是實抓失敗的補位，hot／growth 是根本還沒查，性質不同）
- **落地**：`buildArtifact.py` 的 `scored`／`ranked` 兩個旗標與 `data-ranked`；
  排序 JS 分兩段接起來；頁面加一段說明「—」是「還沒查」不是「不值得投」

### JR-004 · 2026-08-06 · scope
- **決策**：`companies.yaml` 收錄範圍**三層全收**，新增 `purity` 欄位（`core` 自有產品／
  `service` 代理顧問 SI／`adjacent` 主業非行銷科技但職能重疊），**報告預設只排序 `core`**，
  另兩層照列不進四維排名；`category` 同時改為 MarTech Landscape 六大類固定值
- **理由**：主檔從來沒有收錄準則，20 家是「開放平台抓得到的」長出來的——結果 cacaFly
  （媒體代理）、域動在內，同性質的美庫爾 Merkle、安布思沛卻整家不存在，連盲區都沒被記錄
- **棄選**：只收 core（漏掉 dentsu／WPP 系在台的資料職，那是求職者實際會投的）；
  三層一起排名（顧問公司與 SaaS 的熱門度不同義，同榜等於把兩把尺相加）
- **落地**：候選名單已查證 12 家存於 `docs/candidates.md`，主檔改動列待操作第 9 筆

### JR-003 · 2026-08-05 · data
- **決策**：手動來源（104）設 **21 天新鮮度上限**；超過就在整併報表要求把該來源的職缺
  標為「可能已下架」，本期未匯入則自動退用最近一期但**大聲說出來**
- **理由**：手動環節真正的風險不是「忘記做」，而是「六月做過一次，之後每期都在用那份
  舊資料，而報告看起來一切正常」——這是 `martech-trend-agent` 踩過的「舊文佔位」
  在本專案的變形（← 該專案 analysis-2026-08-05 見解 1）
- **棄選**：不設限（過期無感）；硬性拒用過期資料（會讓報告直接少五家，比標示更糟）
- **落地**：`mergeChannels.py` 的 `MANUAL_MAX_AGE_DAYS`；四種情境（無匯入／正常／過期／
  格式不符）皆已實測

### JR-002 · 2026-08-05 · data
- **決策**：跨管道去重鍵用「正規化(公司名 + 職稱)」，且**職稱正規化刻意不動圓括號**，
  只去掉平台自己加的 `【編號】` 與 `[UPD]` 類狀態標籤
- **理由**：Appier 的 `(Ad Cloud Platform)`／`(Playable Ads)`／`(BotBonnie)` 是不同團隊的
  不同職缺，砍掉圓括號會誤併——**實測多吃掉 14 筆真實職缺**（143 → 133）
- **棄選**：用 url 當鍵（同一個缺在不同平台 url 本來就不同，等於沒去重）；
  括號全砍（看起來乾淨，但吃掉真資料）
- **落地**：`mergeChannels.py` 的 `normTitle()`，理由寫進 docstring 防止有人「順手清乾淨」
- **⚠️ 2026-08-13 更正（← JR-007）**：本則舉的例子是錯的。
  `【SGMO0801】Data Analyst` 與 `【TPD0803】Data Analyst` **不是分屬不同團隊的兩個職缺，
  是同一個缺**——Cake 那筆的標題編號過期，它自己的網址寫的就是 `tpd0803`。
  決策方向（編號優先於職稱）仍然成立，但支持它的證據要換成 JR-007 的兩個實例

### JR-001 · 2026-08-05 · analysis
- **決策**：公司「口碑」維度改用**面試趣公司評價實數換算**（≥4.0→5 格、3.5–3.9→4、
  3.0–3.4→3、2.5–2.9→2、<2.5→1），心得數不足者標「—」不給分
- **理由**：原本憑「團隊公開發聲品質」判斷，實抓 9 家後**推翻了其中 3 家**——
  awoo 2.7、CYBERBIZ 2.8、TenMax 2.2，全都比我判斷的低。且面試趣無 bot 防護、
  數字就在 meta 標籤裡，是**唯一能穿透 104 缺口的來源**（員工評價與有無在刊職缺無關）
- **棄選**：維持主觀判斷（不可重跑、無法跨期比較）；用比薪水（欄位較少，只有月薪）
- **代價**：多一個管道要維護；面試趣的公司 code 必須一次性人工建檔
  （搜尋頁是前端渲染，抓不到），已存進 `companies.yaml`
- **邊界**：漸強實驗室心得數不足，口碑標「—」、四維總分以已測 8 家的中位數 3 代入，
  頁面須標註——「沒資料」不等於「口碑差」

---

## 待驗證與待操作事項

> 格式沿用根 `prepare.md` 的「待討論事項」四欄。命名不同是刻意的——
> 這幾筆**不需要決策，只需要去做或去驗**，叫「待討論」會讓它們看起來卡在等人拍板。

| # | 事項 | 卡在什麼 | 提出日期 |
|---|------|---------|---------|
| 1 | `import104.py` 從未對真實 104 頁面跑過 | 三段回退只用合成 fixture 驗過策略 1（JSON-LD）與策略 3（DOM 錨點）；**策略 2（`__NUXT__`）一次都沒命中過**。哪一段會在真實頁面生效，要存一頁下來才知道。同 `scorm-notion-clipper` 待討論 #5 的形狀：選擇器是推測寫的 | 2026-08-05 |
| 2 | 五家只在 104 的公司尚未匯入 | iKala 有明確入口（104 公司頁 `oi6pygw`，已查證是其官網唯一職缺出口）；CYBERBIZ／Vpon／cacaFly／域動需自行搜尋公司頁。**未匯入時報告必須標「未取得」而非「無職缺」**——iKala 與 CYBERBIZ 幾乎確定在招（前者剛與 Google Cloud 合推方案、後者營收 +37% 推 IPO） | 2026-08-05 |
| 3 | cacaFly／Vpon／域動 無面試趣代碼 | 面試趣搜尋頁是前端渲染抓不到代碼，需逐家網路搜尋建檔（既有 9 家花了三輪搜尋）。在那之前這三家的口碑欄永遠空白，四維總分以中位數代入 | 2026-08-05 |
| 4 | 上游新鮮度守門的警告路徑從未執行過 | 2026-08-05 修完守門後，隨即把上游 repo 切回含最新快照的分支才重跑——**守門沒有機會觸發**。下次上游有未合併的資料 PR 時才會第一次真正跑到 | 2026-08-05 |
| 5 | ~~跨期 diff 從未驗證~~ **已驗，但查出去重鍵的侷限** | 三期跑完。去重鍵（平台職缺編號＝URL）本身穩定，但**同一個缺換城市會換 URL**：Appier `Staff/Senior ML Scientist (Ad Cloud)` 上期在東京、本期在台北，刊登日兩期都是 2025-05-19，卻被同時計入新增與消失。本期 14 筆「新增」有 3 筆屬此類。**判讀差集必須配合刊登日**，已寫進報告 §01 與 §07 | 2026-08-05（2026-08-13 結案） |
| 6 | `--no-yourator` 旗標未單獨測過 | 其餘三個（`--no-cake`／`--no-rep`／`--no-upstream`）實跑中都用過，這個是新增後沒機會用到 | 2026-08-05 |
| 7 | Meet.jobs 尚未接入 | 嘗試的端點（`/zh-TW/jobs`、`/zh-TW/companies`、`/api/v1/jobs`）全回 404，入口待重找 | 2026-08-05 |
| 8 | Lever／Ashby 已探測可用但未接 | `api.lever.co/v0/postings/<token>` 與 `api.ashbyhq.com/posting-api/job-board/<token>` 都有回應（實測 Pinkoi 4 筆、Iterable 28 筆），但台灣 MarTech 目前無人使用。等擴到新加坡／日本職缺時再接 | 2026-08-05 |
| 9 | 新收的 15 家全部沒有 `hot`／`growth`／`interviewCode` | 依 JR-005 留空，所以這 15 家目前**一律不進排名**——名單覆蓋補上了，排名覆蓋還沒。要進榜需逐家查營收／募資／上市狀態（`growthNote` 要寫得出證據）＋ 人工搜面試趣代碼。建議一次只補 3～5 家，每補一批重跑 `buildArtifact.py` 看榜有沒有被灌歪 | 2026-08-06 |
| 10 | `docs/candidates.md` 還剩 12 家待查證 | 安布思沛、iCHEF、三竹資訊、創市際、精誠 Etu、QSearch、TAmedia、Criteo／Taboola／AppsFlyer 台灣，以及**業務範圍未確認**的就是廣告科技（`just-adtech`）與數字廣告（`digitm`）——後兩家有 Yourator slug 但查不出主業，`purity` 判不出來就不進主檔 | 2026-08-06 |
| 11 | adGeek 從 5 個缺歸零，原因未確認 | 上期 5 筆全在 Yourator 且全標「一個月內更新」，本期同 slug 抓到 0 筆。五個缺同時消失且集中單一管道，像整批下架或帳號調整，但**沒有證據能分辨「停招」與「換平台」**——adGeek 無其他開放管道可交叉驗證，官網徵才頁本工具未解析。下期若仍為 0 且 104 也查不到，才可往「停招」方向寫。**2026-08-19：連兩期 0 筆，但 104 仍未匯入，升級條件未滿足，維持「推測」不動。** 同期 inline 也由 1 → 0，開放平台 0 筆的公司 18 → 19 家。**2026-08-31：連三期 0 筆，104 仍未匯入，第三期維持不升級**；開放平台 0 筆的公司仍是 19 家、**名單與上期逐家相同** | 2026-08-13 |
| 12 | 「主檔外出現過的公司」從未被系統性回收 | 2026-08-13 發現**奧美整合行銷傳播集團職缺量全資料集第 2 多（30 筆），卻連候選檔都沒進**——它一直在上游的產業關鍵詞掃描結果裡，只是沒人回頭看。同批還撈出 SOOCKER、睿勝科技、伊原力、WitsPer 四家未記錄的公司。**下次擴充名單前先跑一次「主檔外公司」清單**，比憑印象想名單可靠 | 2026-08-13 |
| 13 | 口碑抓取頻率應由「每期」改為「每月」 | 連三期實測：分數（公司評價／面試評價／難度／年薪中位／工時）**一次都沒變過**，會動的只有心得數，而且是個位數、還會上下浮動（iKala 287→286→287）。2026-08-19 六天內只有 5 家各 +1。**2026-08-31 隔了 12 天（前三期是 3～6 天），分數還是一格都沒動**，只有心得數 Appier +2／SHOPLINE +1／91APP +1／CYBERBIZ +2，iKala 連兩期停在 287。連四期同一結論。口碑層的真實解析度是**月**，跟職缺同頻抓既沒有增量資訊，也不符抓取禮儀。改法很小（`run.py` 加一個「本月已抓過就跳過」的判斷），但它會改變 `data/reputation/<日期>.json` 的期別密度，跨期讀取的地方要一起看 | 2026-08-19 |
| 14 | awoo 的重複計數要不要修、修哪一層 | 已立 `JR-010` 記下三層根因，本期刻意先不改。**下期要看的是有沒有第二個實例**——條件是「同一家公司同時出現在兩個管道，且職稱不帶平台無關編號」。若只有 awoo 一家，候選改法 (a) 公司別名表的成本最低、風險也最低；若出現第二家且形態不同，就要重新評估 (b)。**在改之前，274 這個數字要一直帶著「約 7 筆重複」的註記** | 2026-08-31 |
