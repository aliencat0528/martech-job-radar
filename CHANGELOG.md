# Changelog

格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.0.0/)。

> 本檔於 v1.1.1 補建，v1.0.0 與 v1.1.0 的條目由 README 版本歷史與 git log 回填。

## [1.3.2] - 2026-08-31

### Added
- **2026-08-31 第 5 期快照**：整併後 **274 筆**（Yourator 99、Greenhouse 82、
  上游 Yourator 60、Cake 33，去重前 324）／37 家／口碑 9 家。
  `data/2026-08-31/jobs_final.json`、`report/artifact-2026-08-31.html`
- **JR-010**：去重鍵在「同一家公司同時刊在兩個管道」時可能完全失效（見 Fixed 的說明；
  **本期刻意先立不改**）

### Changed
- **§01 全面改寫**，本期的主軸是三件事：去重鍵的漏洞、awoo 一次開九列、
  Appier 收掉兩個台北 ML 缺
- **§05 改引用上游第 8 期**（2026-08-31）的分析。該期把跨期禁令推到最乾淨的一次驗證：
  間隔從 6 天**翻倍到 12 天**，文章重疊卻從 14／60 掉到 **4／66**（方向相反），
  六個快速源重疊第五期維持 0
- **§05「從 SEO 到 GEO／LLMO」證據升級**：上期依據是 awoo JD 內文，
  本期它進到**職稱**——`品牌社群與口碑行銷主管（AI SEO）`（同一個網址只加四個字）、
  新開 `SEO/GEO 初階工程師`。`SEO/GEO` 這個字串**八期快照裡第一次出現**
- **§06 投遞優先序**：`Digital Advertising Data Analyst` 連兩期排第 0 位（已掛 21 天，
  9/10 滿一個月是判斷分界）；awoo 那條從「GEO／內容線」改寫成「SEO/GEO 線」並加註
  初階職只在嘉義與高雄、台北沒有
- **§05 觀察 C 加註**：The Trade Desk 台北的「2 個總監級商務職」是 2026-08-05 的人工查證，
  本期未重驗；它在四個管道**連五期 0 筆**，屬覆蓋缺口
- 各章數字更新：274 筆；Appier 86→82（台北 53→52）；SHOPLINE 31→32；
  Omnichat 7→4；awoo 4→**14 列**；數據科學／工程 25→18；商務／行銷／客戶成功 115（持平）

### Fixed
- 尚無程式修正。**本期查出的去重漏洞刻意不動手**（← JR-010）：
  awoo 14 列最多只對應 **7 個實際職缺**，274 筆裡約 7 筆是重複計數（2.6%）。
  三層根因——① 去重鍵優先用平台職缺編號，awoo 的職稱不帶編號故退回用網址，
  而 Cake 與 Yourator 的網址天生不同；② 公司名 `awoo` vs `awoo 阿物科技` 對不起來；
  ③ `normTitle()` 刻意不動圓括號（← JR-002），但 `（台北辦公室）` 是地點不是團隊。
  **每一層單獨看都是對的決策**，JR-002 已因舉錯例子被更正過一次（← JR-007），
  這次先讓它跑一期看有沒有第二個實例再改

### Notes
- **本期推薦從 20 筆掉到 18 筆，少的兩筆有明確去向**：Appier 收掉台北的
  `Machine Learning Scientist`（`gh-1501746`，**2019-01-10 刊登、連七期都在**）與
  `Senior Machine Learning Scientist (Enterprise Solution)`。
  不是門檻變嚴，是候選池真的少了兩個缺
- **2019 年那組常駐缺第一次少人**：同期下架的還有 `Campaign Management Intern`（2019-04-01），
  Appier 台北刊登於 2024 年前的缺從 **10 個掉到 8 個**。
  上期上游把這批寫成「不動的基本盤」，一期就破了——**常駐缺不是不會變的背景值**
- **awoo 從開放平台 0 筆變成一次開九列**：Yourator 編號 `ytor-48066`～`48074` 連號，
  是近期一次刊登的一批（三種角色 × 三個城市）。其中 `SEO/GEO 初階工程師` 明寫「初階」，
  是本期資料裡少見的初階技術職，但**只在嘉義與高雄**
- **口碑連四期分數一格未變，而且本期隔了 12 天**（前三期是 3～6 天）。
  只有心得數動：Appier 501→503、SHOPLINE 538→539、91APP 809→810、CYBERBIZ 344→346。
  待驗證 #13 因此從「建議」再往前一步，但仍未動程式
- **adGeek 連三期 0 筆，104 仍未匯入，升級條件第三期未滿足**，維持「推測」不寫停招。
  開放平台 0 筆的公司仍是 19 家，**名單與上期逐家相同**
- **本期新增一條判讀規則（來自上游）**：「新增」欄位裡混著**重新上架的舊缺**。
  Appier 的 6 筆新增裡有 3 筆刊登日是 2025-03-02／2025-11-11／2026-01-29 且八期從未出現過。
  刊登日早於第一份快照（2026-07-19）的一律不算新開編制

## [1.3.1] - 2026-08-19

### Added
- **2026-08-19 第 4 期快照**：整併後 **280 筆**（Yourator 145、Greenhouse 86、
  上游 Yourator 58、Cake 35，去重前 329）／37 家／口碑 9 家。
  `data/2026-08-19/jobs_final.json`、`report/artifact-2026-08-19.html`
- **JR-009**：跨期差集的鍵定為**網址**，不是職稱（見 Fixed）

### Changed
- **§01 全面改寫，並收回一條上期的結論**：上期本報告 §01／§04／§05 三處都寫了
  「初階在收、資深在開」。上游 `martech-trend-agent` 2026-08-19 撤回了這個框架，
  本報告跟著收回，範圍縮到「`ML Scientist (LLM & Agents)`（非 Senior）連三期缺席」這一筆
- **§05 改引用上游第 7 期**（2026-08-19）的分析，並記下一件事：
  上一版說「兩份報告各自獨立走到同一個結論、可信度較高」是**錯的**——
  兩邊當時用的是同一筆證據（同一個 `gh-8118867`），從來不是獨立驗證
- **282 → 280 是本工具第一個沒有口徑雜訊的跨期數字**：公司主檔未增減（37 家）、
  整併鍵未改、四管道全跑完。前三期的總數變化都混著名單擴充或鍵的修改
- 各章數字更新：280 筆；Appier 88→86（台北 55→53）；awoo 6→4；
  開放平台 0 筆的公司 18→19 家（新增 inline）

### Fixed
- **跨期差集用錯鍵，本期會多報 5 筆假異動**（← JR-009）。整併（去重）本身沒問題，
  JR-007 已是用網址比對；出問題的是**報告每期手算的「新增／消失」**，前三期都用
  「公司＋職稱」當鍵。同一份資料：以職稱為鍵是 +19／−21，以網址為鍵是 **+14／−16**，
  差的 5 筆全是**同一個網址只改了標題**：
  - Appier `Senior Digital Advertising Data Analyst` → `Digital Advertising Data Analyst`
  - WebComm `(資深)數據工程師 (Senior) Data Engineer` → `數據工程師 Data Engineer`
  - awoo `SEO 內容文案（嘉義）` → `（嘉義/高雄）`
  - 樂播科技 2 筆（`專案助理`→`行銷專案助理`、`直播行銷專案經理`→`行銷專案經理`）

  照舊算法，本期會寫成「Appier 收掉一個資深分析職、同時新開一個分析職」——那是同一個缺

### Notes
- **本期對求職最直接的一條**：`gh-8118867` 把標題的 `Senior` 拿掉，而 **JD 一字未改**
  （2501 對 2501 字元、逐行 diff 零差異），全篇沒有年資門檻。
  它是四期資料裡唯一「近 30 天新開 ＋ 台北 ＋ 分析職」的缺，本期升到 §00 推薦第一列，
  §06 投遞優先序也改列為第 0 位
- **口碑六天只動了 5 個心得數，9 家分數一格都沒變**（Appier 499→501、SHOPLINE 537→538、
  91APP 808→809、iKala 286→287、CYBERBIZ 343→344）。連三期同一結論，
  「口碑改低頻抓取（每月一次）」從觀察升級為建議，已記入 `prepare.md` 待操作 #13
- **adGeek 連兩期 0 筆，但不升級為「停招」**：上期自訂的升級條件是「下期仍為 0
  **且 104 人工匯入也查不到**」，本期 104 未匯入，條件沒滿足
- **SHOPLINE 的 5 個資料職連四期零流失**，職缺編號都沒換（TPD0603／CMS4001／
  SGMO0801／TENG1502／TENG1402）
- 純「數據分析」職稱四期為 4 → 4 → 5 → 5，其中 2 個是 Appier 2019 年的常駐缺——
  **全台「今天真的在收人」的純分析職只有 3 個**

## [1.3.0] - 2026-08-13

### Added
- **名單補進 2 家**（招募出口皆實查過）：
  - **奧美整合行銷傳播集團**（廣告推廣／`service`／Yourator `ogilvy`）——
    **本期職缺量全資料集第 2 多，僅次於 Appier，卻從來沒進過候選檔**。
    先前只靠上游關鍵詞掃描順帶撈到 12 筆，建檔後逐家查詢得到 **30 筆**
  - **QSearch 數解人意科技**（社群關係／`core`／Yourator `QSearch`＋Cake `qsearch`）——
    社群聲量分析，與已建檔的意藍 eLand 同賽道。兩個管道公司頁都在，但本期皆 0 筆在刊
- **收錄準則明文化**：能不能歸進 MarTech Landscape 六大類之一？能就收、依營收來源定 `purity`，
  不能就不收。寫進 §02 與 `docs/candidates.md`。本期擋掉的例子：開必拓數據（AI 製造檢測）、
  緯雲（體育遊戲系統）——它們的職缺一直出現在資料裡，但六大類沒有一格放得下

### Changed
- **§02 分層視覺反轉**（← JR-008）：`core` 從「沒有徽章」改為**實心 accent 徽章＋左側色條**，
  `service`／`adjacent` 各給可辨識但低調的樣式；表格改為**依 `purity` 分三組**、加組標題，
  排序按鈕只在組內作用，不再把三層混排
- 各章數字更新：282 筆／37 家；**可比子集重新啟用**（254 → 252，−2），
  266 → 282 的 +16 幾乎全來自名單擴充，不是市場成長

### Fixed
- **`.p-service` 與 `.p-adjacent` 兩個 class 從來沒有對應的 CSS 規則**
  （`template.html` 只定義了 `.ptag`），兩層外觀完全相同、分不出來。本版補上

### Notes
- 本期快照分兩次抓取（上游與口碑在補名單前、Cake／Yourator 在補名單後重抓），
  同日內的職缺異動會被記進來——簡訊設計 SimpleInfo 的 −2 就是這樣來的
- 待操作新增第 12 筆：**「主檔外出現過的公司」從未被系統性回收**。
  奧美就是這樣漏掉的，同批還撈出 SOOCKER、睿勝科技、伊原力、WitsPer 四家未記錄公司

## [1.2.1] - 2026-08-13

### Fixed
- **整併鍵同時犯了兩個方向相反的錯，三期都在錯**（← JR-007）。由用戶指出推薦區有重複職缺追出來：
  - **漏併**：SHOPLINE `Data Analyst` 在 Cake 標題寫【SGMO0801】、Yourator 寫【TPD0803】，
    兩筆 JD 內文一字不差，是同一個缺卻各算一筆。舊鍵只讀標題編號，
    而 Cake 那筆的網址就寫著 `/jobs/tpd0803data-analyst`
  - **誤併**：Appier `Staff/Senior ML Scientist (Ad Cloud)` 台北（`gh-6897205`）與
    東京（`gh-7092458`）標題相同，沒有編號時舊鍵退回比職稱，兩個真實職缺被併成一筆，
    且勝出者取決於迭代順序——跨期比較因此看到它在台北／東京間跳動
  - 改為「**任一識別碼相同即同一個缺**」的分群：標題編號與網址編號都收，
    沒有編號時用平台識別碼、**不退回職稱**
- **三期資料以新鍵重算**：196／266／266（原 196／265／265）。全 266 筆掃描後
  **殘留重複組數為 0**

### Changed
- **§00 改為三層**（← JR-006 修訂）：A 主推薦（三項齊備）／B 次選（缺一項並標明）／
  觀察名單（公司夠格但本期無範圍內職缺，附原因）。單層門檻讓整張表只剩 2 家，
  把「查不到」與「不夠好」混成同一件事
- **§00 新增「管道」欄**：同一個缺跨管道整併後只留一列，管道欄寫「Cake＋Yourator」
  並註明「兩個管道皆有，同一個缺」，讓讀者分得出交叉確認與重複列出
- **§00 職缺範圍放寬**：加入 TSE 技術顧問與職稱帶 data／數據的產品職——
  這兩條是報告 §06 自己列的入行路徑
- 各章數字隨重算更新：純分析職 4 → 4 → 5（原記 5 → 5 → 6）、
  SHOPLINE 資料職 5 個（原記 6 個，多的那個就是重複那筆）、Appier 88 筆

### Notes
- **收回上一版的一條判讀**：v1.2.0 §01 寫「同一個缺換城市會被算成一增一減」，
  那是整併誤併的假影，不是真實現象。真實的異動是 13 進 13 出
- **JR-002 舉的例子也一併更正**：當時說 SGMO0801 與 TPD0803「分屬不同團隊」是錯的。
  決策方向（編號優先於職稱）仍成立，證據換成 JR-007 的兩個實例

## [1.2.0] - 2026-08-13

### Added
- **報告 §00「推薦我投的職缺」**（`report/buildArtifact.py` 的 `recommendRows()`
  ＋ `template.html` 的 `#pick` 區塊）。三個門檻同時成立才進榜：
  口碑 `repScore ≥ 3` 且心得數 ≥ 50、`growth ≥ 4`（留空者不給預設分，故不進榜）、
  職缺歸類為「數據分析」或「數據科學／工程」且工作地在台灣。
  本期輸出 18 筆／2 家（近期新開 4、常駐缺 7）
- 排序**刻意不用四維總分**：四維是「公司」的分數，拿它排「職缺」會把 Appier 那批
  2019 年掛到現在的常駐缺排在新開的缺前面。實際排序為刊登新鮮度優先，
  同級才比公司四維總分；刊登超過 365 天者標「常駐缺」、30 天內者標「近期新開」
- §00 附「誰沒進榜、為什麼」清單——由排除原因自動生成，
  區分「口碑樣本不足」「未達門檻」「職缺未取得」三種，
  因為**沒進榜的原因多半是查不到，不是不好**

### Changed
- 報告更新到**第 3 期（2026-08-13）**：35 家、265 筆。頁首、§01 判斷、§04 統計、
  §05 趨勢引用、§06 投遞優先序、§07 覆蓋限制、§08 更新表全數改寫
- §05 標明**期別落差**：上游 `martech-trend-agent` 的資料層本期已刷新到 08-13，
  但分析層未跑，`analysis-*.md` 仍停在 08-10，因此引用的是 08-10 的結論
- README 版本歷史與功能特色補上 §00；badge 升到 1.2.0

### Fixed
- Yourator 的相對時間（「一個月內更新」）在 §00 被重複加上「更新」標籤，
  印成「一個月內更新更新」。改為只有值是真日期時才附標籤

### Notes
- **本期名單未變動，265 → 265 是第一次可直接相減的跨期比較**，
  v1.1.1 的可比子集限制解除
- **去重鍵的侷限（本期查出）**：去重用平台職缺編號（等同 URL），
  同一個缺換城市會被算成一增一減。實際案例：Appier
  `Staff/Senior Machine Learning Scientist (Ad Cloud)` 上期在東京、本期在台北，
  刊登日兩期都是 2025-05-19。本期 14 筆「新增」裡有 3 筆屬此類（另兩筆是舊缺重新上架）。
  判讀差集必須配合刊登日，否則會把重新上架當成新開編制
- **口碑更新頻率的修正**：v1.1.1 記錄「五天內一個數字都沒動」，本期三天後再抓發現
  心得篇數會動（Appier 497→499、iKala 287→**286**，會減少），但公司評價（一位小數）
  九家全部沒變。結論修正為「會動的是樣本數，不是分數」

## [1.1.1] - 2026-08-10

### Fixed
- **`report/buildArtifact.py` 的 adGeek 職缺在頁面上重複兩次**：`<!--ADGEEK-->` 原本呼叫
  `jobTable()` 兩次（`adGeek／Atelli 艾得利` 與 `adGeek`），但 `matchCompany()` 是前綴比對，
  第一次呼叫已經把兩種公司名的資料都吃進去了。改為只呼叫一次。
  2026-08-05 那期兩個名稱各有資料所以沒被發現，本期資料全部集中在其中一個名稱才顯形

### Added
- `docs/ARCHITECTURE.md`：四層架構圖、模組職責、去重鍵與跨期比較的前提、與上游的邊界
- 本檔（`CHANGELOG.md`）

### Changed
- 報告更新到 **第 2 期（2026-08-10）**：35 家、265 筆職缺。`report/template.html` 的
  §01 判斷、§04 統計、§05 趨勢引用、§07 覆蓋限制全數改寫
- README 的資料來源限制：104 未取得的公司從 5 家更正為 **7 家**（名單擴充後新增
  美庫爾 Merkle、達摩媒體），家數以 `mergeChannels.py` 印出的清單為準

### Notes
- 本期是第一次有可比的上一期，但主檔在 08/06 從 20 家擴到 35 家，
  196 → 265 的差額幾乎全來自名單。跨期判讀限定在「上期就已在名單裡的公司」子集（196 → 190）

## [1.1.0] - 2026-08-06

### Added
- `companies.yaml` 新增 `purity` 三層（`core` 自有產品／`service` 代理顧問／`adjacent` 相近非純）
  與 MarTech Landscape 六大類固定 `category`，三層全收但**報告只排序 `core`**（← JR-004）
- `mergeChannels.py` 新增 taxonomy 守門：`category`／`purity` 寫錯值會在單一入口出聲
- `docs/candidates.md`：查過但還沒建檔的候選公司

### Changed
- 公司名單 20 → 35 家，補收美庫爾 Merkle、OakMega、意藍資訊、LnData、meepShop 等
- **未評分的公司不再拿預設分進榜**（← JR-005）：`hot`／`growth` 留空者照列但不排名，
  `#` 欄標「—」。舊行為 `c.get("hot", 2)` 會讓沒研究過的公司帶著憑空的 2 分擠掉前段班

## [1.0.0] - 2026-08-05

### Added
- `companies.yaml` 公司主檔（20 家公司、9 家面試趣代碼）
- 抓取層：`fetch/fetchCake.py`、`fetch/fetchYourator.py`、`fetch/fetchRep.py`，
  加上唯讀取用 `martech-trend-agent` 快照
- `import104.py`：104 人工匯入，解析使用者自己另存的本機頁面，三段回退、不連網
- `mergeChannels.py`：單一入口守門（欄位契約＋跨管道去重＋新鮮度檢查）
- `report/buildArtifact.py` ＋ `report/template.html`：資料驅動的報告生成器，無手抄數字
- 2026-08-05 的 196 筆基準快照

### Fixed
- 去重鍵從「公司＋職稱」改為平台職缺編號——SHOPLINE 兩個不同團隊、同名的
  `Data Analyst` 原本會被併成一筆
- 上游快照新鮮度守門：上游 repo 的工作區停在不含最新快照的分支時，
  本工具會安靜地用到舊資料且無錯誤訊息

### Removed
- 104 自動抓取——端點有 Cloudflare bot 防護，不做規避，改走人工匯入
